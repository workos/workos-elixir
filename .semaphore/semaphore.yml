version: v1.0
name: Elixir

agent:
  machine:
    type: e2-standard-2
    os_image: ubuntu1804

blocks:
  - name: Run Tests 👩🏽‍🔬
    task:
      jobs:
        - name: Elixir Tests
          commands:
            - checkout
            - sem-version elixir 1.11.4
            - mix local.hex --force
            - mix local.rebar --force
            - mix deps.get
            - mix test

promotions:
  - name: Release to Hex 🚀
    pipeline_file: .semaphore/release_to_hex.yml
